<svg width="90" height="24" viewBox="0 0 90 24" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Aspose (placeholder logo — replace before production)">
  <!-- PLACEHOLDER ONLY. Replace this file with Aspose's actual official
       logo SVG/PNG from your internal brand assets before deploying.
       This is a plain text stand-in, not a reproduction of Aspose's
       trademarked artwork. -->
  <!-- v1.0.2: fill changed from #121820 (v1.0.1's --ink) to #D9DFE5
       (v1.0.2's --code-text) — the header background is now dark
       (--code-bg), and the old dark fill would have been invisible
       against it. Font family updated to match this version's UI font. -->
  <text x="0" y="17" font-family="'Space Grotesk', Inter, system-ui, sans-serif" font-weight="700" font-size="17" fill="#D9DFE5">Aspose</text>
</svg>
